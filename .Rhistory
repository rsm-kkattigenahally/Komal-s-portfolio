---
title: "A Replication of Karlan and List (2007)"
q()
install.packages("mlogit")
install.packages("dfidx")
install.packages("mlogit")
<pre> ```python import numpy as np import statsmodels.api as sm df['ratio1'] = np.where(df['ratio'] == "1", 1, 0) X = df[['ratio1', 'ratio2', 'ratio3']] y = df['gave'].astype(int) X = sm.add_constant(X) model = sm.OLS(y, X, missing='drop').fit() print(model.summary()) ``` </pre>
